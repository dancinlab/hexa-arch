# hexa-arch/archive/ — frozen non-SSOT mirrors

> Read-only historical snapshots. **NOT the SSOT.** Decision 2 holds:
> each mirrored project's live SSOT remains its own repo. These copies
> exist only for preservation / cross-reference inside hexa-arch.

## comb/  (snapshot 2026-05-18)

- **SSOT (live)**: `~/core/hexa-lang/comb/` on branch `rfc043-hexa-torch`
  (hexa-lang repo). This `archive/comb/` is a frozen as-is copy
  (incl. failed/partial work — "실패한거라도" per user) for the record;
  it is NOT edited here and does NOT move ownership out of hexa-lang.
- comb = the n=6-fabric *consumer* of hexa-arch[chip] (Decision 2 /
  Decision 11 meta-conductor). This archive does not change that.
- Provenance: comb commit context at snapshot = hexa-lang
  `rfc043-hexa-torch`; comb's own CLOSURE.md / T3_DESIGN_FINAL.md
  record its honest partial/falsified status (g3 — unchanged here).
