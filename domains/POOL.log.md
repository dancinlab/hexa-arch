# POOL — historical log

Spec at [`./POOL.md`](./POOL.md). Log entries below preserve session-by-session evolution; the spec file holds only the current confirmed state.

## Log

- **2026-05-22 KST** — POOL.md neu. Promotes the existing `~/core/
  pool/` CLI + the `_pool_cli_present` precedent in
  `cross_code_dft.py` to a first-class infrastructure layer doc.
  §1 goal (pool as canonical external-compute routing layer · why
  first-class · 5 reasons) · §2 routing taxonomy (5-row mirror of
  RTSC §8.7 gate pattern — build/CI · heavy science · large-data
  fetch · GPU · sandboxed risky) · §3 honest invariants (pool does
  NOT sync filesystems · R4 carry-over `absorbed=false` 영구 ·
  provenance must cite pool host · re-verify locally · pool is a
  venue not a verb) · §4 cohort routing examples (cross_code_dft
  canonical · NUCLEAR HFBTHO potential · κ-rounds local vs pool
  heuristic · `pool on all` fanout) · §5 invocation patterns
  (canonical wrapper location · 9 verbs · sync mechanism = none ·
  auth = ssh BatchMode · output retrieval = explicit rsync · `pool
  init` 8 features incl. OOM-resilience block · hexa-native port
  blocked on 2 runtime stubs) · §6 anti-patterns (trivial compute ·
  must re-verify locally · don't push from pool · don't embed
  cohort verbs · don't conflate with sync · don't assume host up) ·
  §7 citations (pool CLI itself · existing demiurge pool routing ·
  honesty invariants this doc cites · future axes deferred). All
  rows describe *current* state — Phase 1 wrap-as-is. No
  prescriptive future-pool features.
