# domains/matter/ — hexa-matter absorbed (git-tracked verbatim, D14-hybrid)

> rfc_005 step ② · design.md D12/D13/D14 · 2026-05-18.

- **Source**: `github.com/dancinlab/hexa-matter` @ `f0ecc06` (MIT),
  pushed to origin/main before copy (anti-data-loss).
- **Faithful set = git-tracked tree only.** hexa-matter's local
  agent scratch (`.claude/worktrees/` — dozens of throwaway
  worktree clones, the bulk of the 210 MB), `state/`,
  `__pycache__/`, `*.log` are hexa-matter's own gitignored
  local state (analog of `.git`), NOT the toolkit — excluded.
  This is the honest reading of "verbatim" (the project as its repo
  defines it), not a copy of transient scratch (g3 + "look before
  you absorb").
- **D14 = hybrid**: tracked content preserved verbatim NOW +
  hexa-native dispatcher shim (rfc_048 pattern); per-verb
  hexa-native re-derivation = rfc_006+ follow-on (NOT done).
- **D13**: `domains/matter/` = materials-synthesis stage of the
  meta-conductor chain (rfc_004 §4).
- **g3 (RETAINED, D12) — NOT yet "absorbed"**: copy ≠ parity.
  `absorbed` flips only when hexa-matter selftest 32/32 + 29
  parity gates run GREEN *under this tree* (rfc_005 §4), filed in
  PLAN.md with cited numbers. Until then: program-in-progress.
- Tombstone (rfc_005 §6 ⑤⑥ GitHub rename + local delete): GATED —
  not done; needs ③ selftest-GREEN + ④ dependents + explicit go.
